* histogram (lower_bound/upper_bound)
* sort timezone string by continent while preserving the suffixes order (stable sort)
* given a known list of names, print all the names from the input which aren't in the original list (set_diff)
